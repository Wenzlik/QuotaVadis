QuotaVadis 0.4.2

• Found and fixed the real cause of the Keychain password prompt appearing on its own: both prompt types are gated by a single process-wide macOS switch that our earlier fixes never touched. Background checks now flip that switch off for the app's entire lifetime, so nothing running silently can ever trigger it — only something you do yourself (opening the app, changing a login in Settings) can.
