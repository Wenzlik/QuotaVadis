QuotaVadis 0.4.1

• The Keychain password prompt could still appear on its own, unprompted — the previous fix wasn't enough on some Macs. This is now fixed properly: background checks no longer even attempt a read that could show the prompt, instead of asking macOS nicely not to.
