QuotaVadis 0.4.0

• New menu bar style: swap the flame for 1 to 4 filled bars — each its own window, a spend cap, or the highest usage across every tool. Percent beside each bar or tucked inside; an optional small vendor mark per bar.
• Any window is selectable now, not just the main two — including Cursor's Grok Bot and Claude's per-model weekly windows.
• Antigravity is now labelled Gemini everywhere (same data source, same settings — nothing to redo).
• A tool with no detected login stays out of the panel and menu bar until you actually sign in, instead of showing an empty row.
• iPhone app: real vendor marks on each tool's card.
